// Create an array
let arr = [1, 2, 3, 4, 5]

// Access array elements
let first = arr[0]
let last = arr[4]
print(first)
print(last)

// Arrays can contain mixed types
let mixed = [1, "hello", true]
print(mixed[1])